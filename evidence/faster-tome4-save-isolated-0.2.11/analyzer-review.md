# 独立分析器审查记录

审查日期：2026-09-14。对象为 [analyze_isolated.py](/workspace/t-engine4/tmp/save-isolated-20260914/analyze_isolated.py)，最终 SHA256：

`cd5413722ead0fef6688db7d32fe9983cd5371b8ff8aaf3ab896c0ab6922c704`

分析器实现完成，并通过两段轻量核验的 **58 项显式检查（30 + 28）**。同时，十个已完成 `preflight2` 会话分别通过底层记录校验和完整会话校验。审查结论只涉及分析方法、程序约束及证据格式；尚未形成任何正式性能结论。

## 冻结与开发顺序

正式计划 SHA256 为：

`82ba2eede4edcf0c454a0ecafaada371c421493cb2b6a2fcdc79fa140398a2b3`

统计方法、五个场景各固定 30 对、唯一开关、300 会话顺序、测量边界和输入源码在正式采样前已经冻结。分析器代码在正式采样进行期间完成，最终代码 SHA 单独记录；不能把这个代码完成时间描述为采样前冻结。

开发和本次审查**没有读取任何正式会话的 input/result 内容，也没有计算部分正式样本的性能估计**。只读了计划、runner/probe/helper、方法说明和十个已完成 `preflight2` 的记录。预检 CPU 原始字段仅用于验证采样字段、时间点顺序和区间重算一致性，没有据此计算预检均值、置信区间或优化通过结论。合成测试使用明确人工构造的数值，没有把预检数据复制成正式样本。

本次未运行游戏、压缩测试或性能基准，也未修改计划、runner、probe 或生产代码。完成本记录时未再修改分析器。

## 实现约束

分析器先检查计划中的全部 300 会话均有 input/result 文件；文件缺失时，尚未读取任何会话内容就拒绝继续。随后验证所有会话及配对条件，全部通过后才调用统计函数。没有部分分析、补样、删慢值或扩样入口。

验证包括：

- 同一 `9464fdfc425f53a238aa76ffc16853bd9c5e38df9d6e9b5519941b517f5d2f76` 包、引擎/原档、计划及诊断源码 hash；唯一候选开关、其余完整生产配置、实际 setup 来源和第 62 个名字的编码行为。
- 全局序号、计划声明早于会话、实际开始顺序和不重叠；相同初始保存文件、运行时版本和各场景/变体内的配置 hash。
- 唯一有效完成记录、无 Lua 错误/超时、同步保存返回及原保存完成后的排空条件；原始时间点与记录的各区间重算一致。
- 无事件场景 0 标记、库存场景 2 标记及 24 个对象的规范化 ownership、Fearscape 场景 6 标记及真实退出报告的身份/回调恢复、token 和 trapper 条件。
- 每个会话保存前后语义状态不变；使用冻结 `run.canonical_loaded_state` 对每对 A/B 的前后投影进行比较。比较保留 UID 别名，并区分 JSON 布尔和数值。
- 每个 archive 的显式 CRC、唯一条目名与一个 main 检查；对 `world.teaw`、`yron/game.teag` 这两个必写 archive 检查预期十进制/base62 名字。旧而未重写的 zone archive 不被误作新编码验收对象。

CRC 依据是已冻结 runner 在写出结果前执行的完整归档校验及其记录；分析器不会再次解压 300 次保存。2741 项环境文件 fingerprint 会在所有正式结果齐全后独立复核。程序末尾还复查计划和来源，输出使用完整 JSON 的原子替换。

Lua `tostring` 会分别舍入绝对时间值和区间值，端点一致性检查使用预定义的数值序列化容差。这个容差只用于检查 JSON 读数能否对应原区间，不是计时精度或 CPU 误差上界。

## 统计及公开输出

每个场景的唯一主指标为 `save_process_user_ms`，使用固定 30 个配对 `log(B/A)`、df=29 的 Student t 区间；单侧 95% 上界严格小于 +1% 才通过。恰好 +1% 的常数比值已专门验证为不通过。五个场景独立报告，不合并先前实验。

其他 CPU/wall 指标都接受有限、非负读数，使用 30 个配对绝对差报告均值、中位数及两侧 95% t 区间。基线均值大于零时附带的均值变化百分比明确标为描述性数据，不参与门槛判断。零值不加 epsilon、不取 log、不丢弃；零或恒定差导致退化区间时附带不可证明零 CPU 成本的提示。24 件整批入包仍只是一项观测。

entry/exit 的同步阶段包含在其 observed 阶段中，不会再次相加。`event_and_save` 只验证 probe 已定义的 `inventory_add + save` 或 `exit_observed + save`，不补加 entry，也不替代主保存门槛。

每会话 64 个空 bracket 仅作描述性汇总，不扣除、不当成额外独立事件样本，也不称为误差界。公开 JSON 保留每场景全部 30 对的标量指标、会话 input/result/config hash、时序及 archive 统计；不复制角色状态投影或完整 Fearscape 报告。

## 58 项轻量核验的范围

第一段 30 项显式检查覆盖：无变化/小幅变化/恰好门槛/超门槛的主统计，零事件和恒定绝对差，拒绝不足 30 对、非法零主指标、布尔/负数时间、重复 JSON key，固定计划与顺序/布尔选项检查；十个 `preflight2` 的 setup、阶段、端点、CRC/编码和 Fearscape 报告校验；以及错误阶段和、缺标记、错误 encoder、CRC 失败的拒绝路径。

第二段 28 项显式检查覆盖：恰好 +1% 的严格边界、具有解析预期的 log-ratio 上界与绝对差区间；内存模拟“前 299 项齐全、最后一项缺失”时只读计划而不读任何会话；十个 `preflight2` 经仅在内存中调整会话名/序号/声明时间后的完整 `validate_session` 校验，错误开关和布尔/数值状态差异的拒绝；五组预检 A/B 的规范化投影相等；300 个**纯合成**会话的输出行数、主/辅助指标用途标志、隐私 canary 不被输出，以及 299 个会话不能生成统计。

两段使用的是同一组十个已完成 `preflight2`，不是二十个不同预检。合成测试和预检均不计入正式 150 对；它们不证明本机正式性能会通过 CPU 门槛。

## 最终运行入口

仅在全部 300 会话完成后运行：

```sh
python3 -B /workspace/t-engine4/tmp/save-isolated-20260914/analyze_isolated.py \
  --output /workspace/t-engine4/tmp/save-isolated-20260914/results.json
```

输出 `schema=1`。各场景判定在 `cases[case].save_cpu_decision`；主结果在 `cases[case].metrics.save_process_user_ms` 的 `paired_geomean_change_pct` 和 `one_sided_95pct_upper_change_pct`；完整配对行在 `paired_values`。当前没有正式结果可供本记录解释或背书。
